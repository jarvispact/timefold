{
  "name": "@timefold/examples",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev:pong": "sed 's/{example-folder}/pong/g' index.html.template > index.html && vite --port 9093",
    "build:pong": "sed 's/{example-folder}/pong/g' index.html.template > index.html && vite build",
    "dev:animation": "sed 's/{example-folder}/animation/g' index.html.template > index.html && vite --port 9093",
    "build:animation": "sed 's/{example-folder}/animation/g' index.html.template > index.html && vite build",
    "dev:platformer": "sed 's/{example-folder}/platformer/g' index.html.template > index.html && vite --port 9093",
    "build:platformer": "sed 's/{example-folder}/platformer/g' index.html.template > index.html && vite build",
    "dev:webgpu": "sed 's/{example-folder}/webgpu/g' index.html.template > index.html && vite --port 9093",
    "build:webgpu": "sed 's/{example-folder}/webgpu/g' index.html.template > index.html && vite build",
    "dev:engine": "sed 's/{example-folder}/engine/g' index.html.template > index.html && vite --port 9093",
    "build:engine": "sed 's/{example-folder}/engine/g' index.html.template > index.html && vite build",
    "dev:sdf": "sed 's/{example-folder}/sdf/g' index.html.template > index.html && vite --port 9093",
    "build:sdf": "sed 's/{example-folder}/sdf/g' index.html.template > index.html && vite build",
    "dev:perf": "sed 's/{example-folder}/perf/g' index.html.template > index.html && vite --port 9093",
    "build:perf": "sed 's/{example-folder}/perf/g' index.html.template > index.html && vite build",
    "dev:bitmasks": "sed 's/{example-folder}/bitmasks/g' index.html.template > index.html && vite --port 9093",
    "build:bitmasks": "sed 's/{example-folder}/bitmasks/g' index.html.template > index.html && vite build",
    "preview": "vite preview --port 9191"
  }
}